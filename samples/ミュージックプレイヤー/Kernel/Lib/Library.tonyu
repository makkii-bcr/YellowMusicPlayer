extends Object;

function onLoad() {
// ライブラリが読み込まれたときの動作（リソース登録など）
}

native _n_registerResource;
function registerResource(name) {
//　リソースを登録する
  _n_registerResource(name);
}

function onLoadPage(pageName,designMode) {
// ページを読み込んだときの動作
}

function onStop() {
// 実行停止時の動作
}


extends Array;

constructor Matrix(c) {
  super();
  cols=c;
}

function addRows(r) {
  var x,y;
  for (y=0 ; y<r ; y++) {
   for (x=0 ; x<cols ; x++) add(null);
  }
}

function rowCount() {
  
}

function get(x,y) {
  return super.get(trunc(x)+trunc(y)*cols);
}

function set(x,y,t) {
  super.set(trunc(x)+trunc(y)*cols,t);
}